# ============================================================
# Netlify build configuration for React SPA
# ============================================================

[build]
  command = "npm run build"
  publish = "dist"

# SPA client-side routing — serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
