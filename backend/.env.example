# ── Supabase ──────────────────────────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
# MUST be service_role — NOT the anon key from .env.local (Dashboard → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-secret

# ── Gemini AI (free tier — https://aistudio.google.com/apikey) ──
GEMINI_API_KEY=

# ── Tuya Smart Plug (optional — leave blank if no hardware) ──
TUYA_ACCESS_ID=
TUYA_ACCESS_SECRET=
TUYA_API_ENDPOINT=https://openapi.tuyain.com
TUYA_DEVICE_REGION=in

# ── Server ────────────────────────────────────────────────────
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
TIMEZONE=Asia/Kolkata

# ── APScheduler persistence (uses Supabase connection string) ─
# For PoC we use in-memory. For prod, set this:
# SCHEDULER_DB_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
